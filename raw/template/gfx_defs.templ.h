// graphics defines. filled in with constants set in the Makefile

#pragma once


// the most tiles allowed for the UI (we pack sprite tiles after this)
#define MAX_UI_TILES    ({max_ui_tiles})

#define FIRST_SPRITE_TILE   MAX_UI_TILES
#define MAX_SPRITE_TILES    ({max_sprite_tiles})
